// setIconBase()
// **********************************************************
// Sets all base rules in order to use icons on this element
// Does not include the icon itself
// Can be useful if you want to optimise your code.
// **********************************************************

setIconBase()
  font-family $ws_icon-font-family
  speak none
  font-style normal
  font-weight normal
  font-variant normal
  text-transform none
  line-height 1
  -webkit-font-smoothing antialiased
  -moz-osx-font-smoothing grayscale

// - setIconInclude( $icon: 'icon-name' )
// **********************************************************
// Includes the icon's content value.
// $icon is the first param of the icon you want to use
// as defined in config/icons.
// **********************************************************

setIconInclude($icon)
  for $s in $ws_icon-list
    if $s[0] == $icon
      content: $s[1]



// - icon( $icon: 'icon-name' )
// **********************************************************
// Includes an icon and its base.
// $icon is the first param of the icon you want to use
// as defined in config/icons.
// **********************************************************

icon($icon)
  setIconBase()
  setIconInclude($icon)
